syms f(x, y)f(x, y) = x^2*y;f(3, 2)x_val = 1:10;y_val = 11:20;f(x_val, y_val)dfx = diff(f, x)# Calculate df(x, y) at x = y + 1dfx = diff(y+1, y)syms y(t) aeqn = diff(y, t) == a*y; ## dy/dt =pkg l aycond = y(0) == 5; ## initial condition -> y(0) = 5ySolt(t) = dsolve(eqn, cond)# [t, y] = ode45(odefun, tspan, y0, options)tspan = [0, 5];y0 = 0;[t, y] = ode45(@(t, y) 2*t, tspan, y0);#plot(t, y, 'c-o')clcsyms f(x2, y2)f(x2, y2) = 3*x^3 + 5*x^2 + 7*x;dfx = diff(f, x)for i = 0:1,  # change the value of 2 for successive differentiation, [NOTE : for loop will be like for(i = 0; i <= n, i++)]  f = diff(f, x)end;syms f(x3, y3)f(x3, y3) = 3*x^2*y + 2*x^3;dfx = diff(f, x)